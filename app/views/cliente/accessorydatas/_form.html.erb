<%= form_for[:cliente, @accessorydata] do |f| %>
  <% if @accessorydata.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accessorydata.errors.count, "error") %> prohibited this accessorydata from being saved:</h2>

      <ul>
      <% @accessorydata.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :accessory_id %><br />
    <%= f.collection_select :accessory_id, @accessories, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

