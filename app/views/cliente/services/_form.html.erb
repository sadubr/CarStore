<%= form_for[:cliente,@service] do |f| %>
  <%= error_messages_for @service %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

